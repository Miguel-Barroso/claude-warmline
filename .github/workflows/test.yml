name: tests
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # the historical keep-warm refresh cases read old tags' policy text
          fetch-depth: 0
      - run: ./test.sh
